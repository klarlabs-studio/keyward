apiVersion: v1
kind: Service
metadata:
  name: proctor-sync-server
  namespace: proctor
  labels:
    app.kubernetes.io/name: proctor-sync-server
    app.kubernetes.io/part-of: proctor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proctor-sync-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
